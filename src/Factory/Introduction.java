package Factory;

public class Introduction {
	
	/*
	 * We want to move the object creation logic from our code to a seperate class.
	 * 
	 * We use this pattren when we do not know in advance which class we may need to 
	 * instantiated beforehand and also to allow new classes to be added to the system and handle
	 * their creation without affecting client code.
	 * 
	 * */

}
